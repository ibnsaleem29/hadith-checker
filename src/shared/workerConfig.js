// Single, clearly-marked place holding the Cloudflare Worker's base URL.
//
// Deployed. This is the real, live Worker (Gemini-backed — see worker/src/index.js).
export const WORKER_BASE_URL = 'https://hadith-checker-worker.oomarshah29.workers.dev';
